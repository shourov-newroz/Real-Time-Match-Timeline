# Backend port (default: 4000)
PORT=4000

# Comma-separated list of allowed frontend origins for CORS and Socket.IO.
# Use your exact Netlify URL. Wildcards are supported for preview deploys.
CLIENT_ORIGINS=http://localhost:5173,https://jovial-kashata-cd65b1.netlify.app,https://*.netlify.app
